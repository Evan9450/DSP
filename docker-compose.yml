services:
  web:
    build:
      context: .
      args:
        NEXT_PUBLIC_API_BASE_URL: http://api:8000
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      NEXT_PUBLIC_API_BASE_URL: http://api:8000
    depends_on:
      - api
    restart: always
